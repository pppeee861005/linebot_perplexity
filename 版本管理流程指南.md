# 版本管理流程指南

## 目錄
1. [版本號管理原則](#版本號管理原則)
2. [發佈流程](#發佈流程)
3. [分支版本策略](#分支版本策略)
4. [Tag 管理實務](#tag-管理實務)
5. [程式碼版本更新](#程式碼版本更新)
6. [版本衝突解決](#版本衝突解決)
7. [實務案例](#實務案例)

---

## 版本號管理原則

### 語意化版本 (Semantic Versioning)

版本號格式：`MAJOR.MINOR.PATCH`

#### 何時增加版本號

**MAJOR (主要版本)**：破壞性變更
- API 不相容的修改
- 移除功能
- 重大架構改變
```
範例：v1.0.0 → v2.0.0
```

**MINOR (次要版本)**：新增功能
- 向下相容的新功能
- 新增 API
- 增強現有功能
```
範例：v1.0.0 → v1.1.0
```

**PATCH (修補版本)**：錯誤修復
- 向下相容的錯誤修復
- 安全性修復
- 小型改善
```
範例：v1.0.0 → v1.0.1
```

#### 預發佈版本標籤
```
v1.0.0-alpha    # 內部測試版
v1.0.0-beta     # 外部測試版
v1.0.0-rc.1     # 發行候選版
```

### 版本號一致性原則

#### 📍 **關鍵原則**
1. **Tag = 程式碼版本 = 發佈版本**
2. **所有分支版本資訊保持同步**
3. **發佈前必須更新所有版本標記**

#### 🔍 **檢查點**
- [ ] 程式碼註解版本 (`# Version: v1.2.0`)
- [ ] 常數版本 (`__version__ = "1.2.0"`)
- [ ] Git Tag (`git tag -l`)
- [ ] README 版本說明
- [ ] 發佈說明文檔

---

## 發佈流程

### 階段 1: 準備發佈

#### 檢查清單
```bash
# 確認分支狀態
git status
git branch --show-current

# 檢查未提交的變更
git diff --name-only

# 確認遠端同步
git fetch origin
git log HEAD..origin/main
```

#### 測試驗證
- [ ] 所有單元測試通過
- [ ] 整合測試完成
- [ ] 手動測試關鍵功能
- [ ] 效能測試通過
- [ ] 安全性掃描完成

### 階段 2: 更新版本資訊

#### 更新程式碼版本
```bash
# 編輯版本常數
vim main.py
# 將 __version__ = "1.2.0" 改為 "1.2.1"

# 更新檔案註解
# Version: v1.2.0 → v1.2.1
# Release Date: 更新日期
```

#### 提交版本更新
```bash
git add main.py
git commit -m "bump version to v1.2.1"
```

### 階段 3: 建立發佈

#### 建立 Tag
```bash
# 建立註解標籤
git tag -a v1.2.1 -m "Release v1.2.1: 新增功能描述

- 新增功能 A
- 修復問題 B
- 改善效能 C"

# 推送標籤
git push origin v1.2.1
```

#### 發佈到生產環境
```bash
# 部署到生產
git checkout v1.2.1
# 執行部署指令...
```

### 階段 4: 發佈後處理

#### 更新文檔
- 更新 CHANGELOG.md
- 更新 README.md 版本說明
- 更新 API 文檔版本

#### 通知相關人員
- 開發團隊
- 測試團隊
- 營運團隊
- 用戶（如果需要）

---

## 分支版本策略

### 分支版本管理原則

#### 🌿 **主分支 (main/master)**
- **版本狀態**：最新穩定發佈版本
- **版本號**：最終發佈版本 (v1.2.1)
- **用途**：生產環境代碼基準

#### 🚧 **開發分支 (develop)**
- **版本狀態**：下一個版本的開發中
- **版本號**：v1.3.0-dev 或 v2.0.0-alpha
- **用途**：整合各功能分支

#### ✨ **功能分支 (feature/*)**
- **版本狀態**：繼承來源分支版本
- **版本號**：來源分支版本 + 功能標識
- **用途**：開發新功能

#### 🐛 **修復分支 (hotfix/*)**
- **版本狀態**：修復特定版本的錯誤
- **版本號**：修復版本 + .1 (v1.2.1)
- **用途**：緊急錯誤修復

#### 📦 **發佈分支 (release/*)**
- **版本狀態**：準備發佈的穩定版本
- **版本號**：即將發佈的版本 (v1.2.0)
- **用途**：最終測試和準備發佈

### 分支版本同步實務

#### 功能分支開發流程
```bash
# 從 develop 分支建立功能分支
git checkout develop
git pull origin develop
git checkout -b feature/new-function

# 開發期間保持版本同步
git checkout develop
git pull origin develop
git checkout feature/new-function
git rebase develop

# 功能完成後
git checkout develop
git merge feature/new-function --no-ff
git push origin develop

# 刪除功能分支
git branch -d feature/new-function
git push origin --delete feature/new-function
```

#### 修復分支流程
```bash
# 從 main 分支建立修復分支
git checkout main
git pull origin main
git checkout -b hotfix/critical-bug

# 修復錯誤
# ... 修復程式碼 ...
git add .
git commit -m "fix: critical bug in authentication"

# 更新版本號 (patch 版本)
# 編輯版本資訊 v1.2.0 → v1.2.1

# 合併回 main 和 develop
git checkout main
git merge hotfix/critical-bug --no-ff
git tag -a v1.2.1 -m "Hotfix v1.2.1: critical authentication bug"

git checkout develop
git merge hotfix/critical-bug --no-ff

# 推送
git push origin main
git push origin develop
git push origin v1.2.1
```

---

## Tag 管理實務

### Tag 命名與使用原則

#### 🏷️ **Tag 類型**

**發佈標籤**
```bash
# 穩定發佈
git tag -a v1.2.0 -m "Release v1.2.0"

# 重大更新
git tag -a v2.0.0 -m "Release v2.0.0: Complete API redesign"

# 修補版本
git tag -a v1.2.1 -m "Release v1.2.1: Security patch"
```

**里程碑標籤**
```bash
# 專案里程碑
git tag milestone/phase-1-complete

# 功能完成
git tag feature/user-auth-complete
```

#### 📋 **Tag 管理操作**

```bash
# 列出所有標籤
git tag

# 依版本排序
git tag --sort=-version:refname

# 搜尋標籤
git tag -l "v1.*"

# 刪除標籤
git tag -d v1.0.0
git push origin --delete v1.0.0

# 重新標記 (小心使用)
git tag -d v1.0.0
git tag -a v1.0.0 <commit-hash> -m "Release v1.0.0"
```

### Tag 與版本同步

#### 🔄 **同步檢查流程**
```bash
# 檢查程式碼版本
grep -r "__version__" .

# 檢查檔案註解版本
grep -r "Version:" *.py

# 比較 Tag 與程式碼版本
git tag --sort=-version:refname | head -5
```

#### ⚠️ **版本不一致處理**
```bash
# 如果 Tag 是 v1.2.1 但程式碼還是 v1.2.0
# 更新程式碼版本
vim main.py
# 將 __version__ = "1.2.0" → "1.2.1"

git add main.py
git commit -m "sync version to v1.2.1"

# 如果需要重新標記
git tag -d v1.2.1
git tag -a v1.2.1 -m "Release v1.2.1"
git push origin v1.2.1 --force-with-lease
```

---

## 程式碼版本更新

### 版本資訊標記方式

#### Python 專案常見位置

**1. 主程式檔案**
```python
#########################################################################
# Python x AI Agent
# Version: v1.2.0
# Release Date: 2025-12-24
#########################################################################

# 應用程式版本資訊
__version__ = "1.2.0"
__author__ = "Developer Name"
__description__ = "Application description"
```

**2. setup.py / pyproject.toml**
```python
# setup.py
setup(
    name="my-package",
    version="1.2.0",
    # ...
)

# pyproject.toml
[tool.poetry]
version = "1.2.0"
```

**3. __init__.py**
```python
# __init__.py
__version__ = "1.2.0"
```

### 自動化版本更新

#### 使用 bump2version
```bash
# 安裝
pip install bump2version

# 設定 .bumpversion.cfg
[bumpversion]
current_version = 1.2.0
commit = True
tag = True

[bumpversion:file:main.py]
search = __version__ = "{current_version}"
replace = __version__ = "{new_version}"

# 使用
bump2version patch  # 1.2.0 → 1.2.1
bump2version minor  # 1.2.0 → 1.3.0
bump2version major  # 1.2.0 → 2.0.0
```

#### 使用 Python 腳本
```python
# version_updater.py
import re
import sys

def update_version(file_path, new_version):
    with open(file_path, 'r', encoding='utf-8') as f:
        content = f.read()

    # 更新 __version__
    content = re.sub(
        r'__version__\s*=\s*["\'][^"\']*["\']',
        f'__version__ = "{new_version}"',
        content
    )

    # 更新註解版本
    content = re.sub(
        r'# Version: v[0-9]+\.[0-9]+\.[0-9]+',
        f'# Version: v{new_version}',
        content
    )

    with open(file_path, 'w', encoding='utf-8') as f:
        f.write(content)

if __name__ == "__main__":
    if len(sys.argv) != 3:
        print("Usage: python version_updater.py <file> <new_version>")
        sys.exit(1)

    update_version(sys.argv[1], sys.argv[2])
    print(f"Updated version to {sys.argv[2]}")
```

---

## 版本衝突解決

### 常見版本衝突情境

#### 情境 1: 多分支版本不一致
```bash
# 檢查各分支版本
git branch -a
for branch in $(git branch -r | grep -v HEAD); do
    echo "=== $branch ==="
    git show $branch:main.py | grep "__version__"
done
```

#### 情境 2: Tag 與程式碼版本不同步
```bash
# 比較最新 tag 與程式碼版本
LATEST_TAG=$(git describe --tags --abbrev=0)
CODE_VERSION=$(grep "__version__" main.py | cut -d'"' -f2)

if [ "$LATEST_TAG" != "v$CODE_VERSION" ]; then
    echo "版本不一致！Tag: $LATEST_TAG, 程式碼: v$CODE_VERSION"
fi
```

### 衝突解決策略

#### 策略 1: 以 Tag 為準
```bash
# 如果 Tag 是權威來源
git checkout v1.2.1
# 確保程式碼版本與 Tag 一致
```

#### 策略 2: 以程式碼為準
```bash
# 如果程式碼版本是正確的
git tag -d v1.2.0
git tag -a v1.2.0 -m "Release v1.2.0"
git push origin v1.2.0 --force-with-lease
```

#### 策略 3: 協商決定
- 團隊討論哪個版本是正確的
- 根據業務需求決定
- 記錄決策理由

---

## 實務案例

### 案例 1: 一般功能發佈

**情境**: 新增使用者認證功能
**目標版本**: v1.1.0

```bash
# 1. 從 develop 分支建立發佈分支
git checkout develop
git checkout -b release/v1.1.0

# 2. 最終測試和修復
# ... 測試和修復 ...

# 3. 更新版本資訊
vim main.py  # 更新為 v1.1.0
git add main.py
git commit -m "bump version to v1.1.0"

# 4. 合併到 main 和 develop
git checkout main
git merge release/v1.1.0 --no-ff
git tag -a v1.1.0 -m "Release v1.1.0: User authentication"

git checkout develop
git merge release/v1.1.0 --no-ff

# 5. 推送
git push origin main
git push origin develop
git push origin v1.1.0

# 6. 清理
git branch -d release/v1.1.0
git push origin --delete release/v1.1.0
```

### 案例 2: 緊急修復發佈

**情境**: 生產環境發現關鍵錯誤
**修復版本**: v1.0.1

```bash
# 1. 從 main 分支建立修復分支
git checkout main
git checkout -b hotfix/security-patch

# 2. 修復錯誤
# ... 修復程式碼 ...
git add .
git commit -m "fix: security vulnerability in user validation"

# 3. 更新版本 (patch)
vim main.py  # v1.0.0 → v1.0.1
git add main.py
git commit -m "bump version to v1.0.1"

# 4. 合併回 main 和 develop
git checkout main
git merge hotfix/security-patch --no-ff
git tag -a v1.0.1 -m "Hotfix v1.0.1: Security patch for user validation"

git checkout develop
git merge hotfix/security-patch --no-ff

# 5. 緊急部署
git push origin main
git push origin develop
git push origin v1.0.1
```

### 案例 3: 重大版本更新

**情境**: API 完全重新設計
**新版本**: v2.0.0

```bash
# 1. 建立新版本開發分支
git checkout develop
git checkout -b feature/api-redesign

# 2. 開發新 API
# ... 大幅修改程式碼 ...

# 3. 準備發佈分支
git checkout develop
git checkout -b release/v2.0.0

# 4. 最終測試和文檔更新
# ... 完整測試套件 ...

# 5. 更新版本資訊
vim main.py  # v1.x.x → v2.0.0
vim README.md  # 更新 API 文檔
vim CHANGELOG.md  # 記錄重大變更

git add .
git commit -m "bump version to v2.0.0 - API redesign"

# 6. 發佈
git checkout main
git merge release/v2.0.0 --no-ff -s ours  # ours 策略保留 main 歷史
git tag -a v2.0.0 -m "Release v2.0.0: Complete API redesign

BREAKING CHANGES:
- All API endpoints changed
- Authentication method updated
- Response format modified"

# 7. 推送和通知
git push origin main
git push origin v2.0.0

# 8. 建立新的 develop 分支
git checkout -b develop-v2
git push origin develop-v2
```

---

## 總結

### 🎯 **版本管理核心原則**

1. **一致性**：Tag、程式碼、發佈版本必須同步
2. **可追蹤**：每個版本都有清晰的變更記錄
3. **可重現**：可以隨時回到任何版本狀態
4. **團隊協作**：版本策略獲得團隊共識

### 📋 **檢查清單**

發佈前檢查：
- [ ] 所有測試通過
- [ ] 版本資訊更新完成
- [ ] 文檔更新完畢
- [ ] 團隊審查完成
- [ ] 備份計劃確認

發佈後檢查：
- [ ] Tag 推送成功
- [ ] 生產環境部署完成
- [ ] 監控系統正常
- [ ] 用戶通知發送

### 🛠️ **工具推薦**

- **版本管理**: bump2version, semantic-release
- **變更記錄**: conventional-changelog
- **發佈自動化**: GitHub Actions, GitLab CI
- **版本檢查**: version-checker 腳本

遵循這個流程指南，你的專案版本管理將會更加規範和可靠！
