# Git 分支與版本管理指南

## 🎯 **避免混淆的實用策略**

### **核心原則**
- **Branch**: 用於開發和修改程式碼
- **Tag**: 用於標記穩定版本

---

## 📋 **分支 (Branch) vs 版本標籤 (Tag)**

| 特性 | 🌿 Branch (分支) | 🏷️ Tag (版本標籤) |
|------|------------------|-------------------|
| **用途** | 開發新功能 | 標記穩定版本 |
| **是否可修改** | ✅ 可繼續修改 | ❌ 永久固定 |
| **切換命令** | `git checkout main` | `git checkout v1.2.0` |
| **推送命令** | `git push origin main` | `git push origin v1.2.0` |
| **生命週期** | 持續變化 | 永久不變 |

---

## 🗂️ **避免混淆的實用策略**

### **策略 1: 簡單的分支命名規則**

#### **推薦的分支命名方式**
```
main          ← 主要的開發分支
feature/*     ← 新功能開發
hotfix/*      ← 緊急修復
release/*     ← 發佈準備
```

#### **你的專案範例**
```
main                          ← 主開發分支
feature/perlpexcity-search    ← 舊版功能分支
```

### **策略 2: Tag 只用於穩定版本**

#### **Tag 命名規則**
```
vMAJOR.MINOR.PATCH
v1.0.0    ← 初始版本
v1.1.0    ← 新功能
v1.1.1    ← 修復錯誤
v1.2.0    ← 新功能 + 修復
```

#### **何時建立 Tag**
- ✅ 程式碼通過測試
- ✅ 準備發佈新版本
- ✅ 修復重要錯誤
- ❌ 隨意標記測試版本

### **策略 3: 清楚的切換規則**

#### **開發時**: 使用 Branch
```bash
# 日常開發用 branch
git checkout main
git checkout -b feature/new-chat
```

#### **部署時**: 使用 Tag
```bash
# 生產環境用 tag
git checkout v1.2.0
```

---

## 🚀 **實用操作指南**

### **查看當前狀態**
```bash
# 查看所在分支
git branch

# 查看可用版本
git tag

# 查看所有參考
git show-ref
```

### **安全切換操作**

#### **切換分支**
```bash
# 安全切換 (有未提交修改時會警告)
git checkout main
git checkout feature/old-version
```

#### **切換版本**
```bash
# 直接切換到穩定版本
git checkout v1.2.0
git checkout v1.0.0
```

### **從穩定版本建立開發分支**
```bash
# 從穩定版本建立新功能分支
git checkout v1.2.0
git checkout -b feature/enhanced-ai

# 從穩定版本建立修復分支
git checkout v1.2.0
git checkout -b hotfix/json-error
```

---

## 📊 **你的專案架構整理**

### **當前狀態**
```
📁 Git Repository
├── 🌿 main (主開發分支)
├── 🌿 feature/perlpexcity-search (備用分支)
├── 🏷️ v1.0.0 (初始穩定版)
└── 🏷️ v1.2.0 (最新穩定版)
```

### **推薦使用情境**

#### **情境 1: 繼續開發新功能**
```bash
git checkout main
# 繼續開發...
```

#### **情境 2: 部署到生產環境**
```bash
git checkout v1.2.0
# 部署這個版本
```

#### **情境 3: 修復舊版本問題**
```bash
git checkout v1.0.0
git checkout -b fix/v1.0-compatibility
# 修復問題...
```

#### **情境 4: 比較版本差異**
```bash
git diff v1.0.0..v1.2.0
git log --oneline v1.0.0..v1.2.0
```

---

## 💡 **實務建議**

### **分支管理**
1. **定期合併**: 將完成的功能分支合併到 main
2. **清理分支**: 刪除不需要的分支
   ```bash
   git branch -d feature/completed
   ```
3. **保持 main 乾淨**: main 應該永遠是可發佈狀態

### **版本管理**
1. **語意化版本**: 遵循 MAJOR.MINOR.PATCH 規則
2. **詳細的 tag 訊息**: 說明這個版本的新功能和修復
3. **定期發佈**: 不要累積太多修改才發佈

### **團隊協作**
1. **統一命名規則**: 全團隊使用相同的分支命名方式
2. **保護重要分支**: main 和 release 分支需要保護
3. **Code Review**: 重要修改需要審核

---

## ⚠️ **常見錯誤與解決**

### **錯誤 1: 在錯誤的分支上開發**
```bash
# 問題: 在 tag 上開發 (會進入 detached HEAD)
git checkout v1.2.0
# 修改程式碼...

# 解決: 建立分支
git checkout -b feature/my-changes
```

### **錯誤 2: 混淆 branch 和 tag**
```bash
# 正確用法
git checkout main        # 切換到分支
git checkout v1.2.0      # 切換到版本

# 錯誤用法
git checkout main        # 以為是最新版本
# 但 main 可能有未完成的修改
```

### **錯誤 3: 忘記推送 tag**
```bash
# 建立 tag 後要推送
git tag v1.2.0
git push origin v1.2.0    # 別忘記這步！
```

---

## 🎯 **總結原則**

1. **Branch**: 開發工具，會變動
2. **Tag**: 版本標記，永不變動
3. **Main Branch**: 保持最新可發佈狀態
4. **Tag**: 只標記經過測試的穩定版本

遵循這些原則，你就能輕鬆管理 Git 的分支和版本了！

---

**建立日期**: 2025-12-24
**適用專案**: LINE AI 聊天機器人
